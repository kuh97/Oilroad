# AGENTS.md — 오일로드 AI Coding Agent 규칙

> 이 파일은 AI Coding Agent가 이 저장소에서 코드를 작성할 때 **반드시 따라야 하는 규칙**입니다.
> 목적은 하나입니다: **AI가 확정된 요구사항·아키텍처·계약을 임의로 바꾸지 못하게 하는 것.**
> 규칙과 코드가 충돌하면 규칙이 이깁니다. 규칙과 사용자 지시가 충돌하면 사용자에게 확인합니다.

---

## 0. 프로젝트 한 줄

출발지·목적지를 입력하면 그 경로에서 주유하기 가장 좋은 주유소를 찾아주되, **경로 반경 밖이라도 우회할 가치가 있으면 찾아내서 경유 경로까지 만들어주는** 서비스.

이 제품의 차별점은 두 줄뿐입니다.

1. 반경으로 자르지 않는다
2. 우회 비용과 이득을 계산해서 순위를 매긴다

**기능을 추가·변경하기 전에 항상 "이게 위 두 줄을 강화하는가"를 먼저 물으십시오.** 아니면 하지 마십시오.

---

## 1. Source of Truth 서열

정보가 어긋나면 아래 순서로 판단합니다.

| 순위 | 문서                   | 관할                                                      |
| ---- | ---------------------- | --------------------------------------------------------- |
| 1    | `AGENTS.md` (이 파일)  | 개발 규칙                                                 |
| 2    | `docs/PRODUCT.md`      | **무엇을** 만드는가 — 기능·도메인 규칙·계산식·파라미터    |
| 3    | `docs/ARCHITECTURE.md` | **어떻게** 만드는가 — 구조·모듈·API Contract·DB·외부 연동 |
| 4    | `README.md`            | 진입점 — 실행 방법·환경변수                               |
| 5    | 코드                   |                                                           |

**같은 정보를 두 문서에 쓰지 마십시오.** 예를 들어 `NetSaving` 계산식은 `PRODUCT.md`에만 있고, 그 계산이 어느 모듈에 사는지는 `ARCHITECTURE.md`에만 있습니다.

**코드와 문서가 다르면 코드를 고치는 것이 기본입니다.** 문서가 틀렸다고 판단되면 코드를 바꾸기 전에 사용자에게 확인하십시오.

---

## 2. Core Rules

1. 기존 요구사항을 임의로 변경하지 않는다.
2. 기존 아키텍처를 임의로 변경하지 않는다.
3. API Contract(`ARCHITECTURE.md` §6)를 임의로 변경하지 않는다.
4. DB Schema(`ARCHITECTURE.md` §7)를 임의로 변경하지 않는다.
5. 외부 API 호출 정책(`ARCHITECTURE.md` §5)을 임의로 변경하지 않는다.
6. 튜닝 파라미터의 **기본값**을 임의로 바꾸지 않는다.
7. 새 라이브러리를 추가하기 전에 기존 의존성으로 가능한지 확인한다.
8. 핵심 계산 로직(`src/domain/`)을 수정하면 반드시 해당 단위 테스트를 함께 갱신한다.
9. 요구사항이 불명확하면 **추측하지 않는다.** §11을 따른다.
10. 기획과 코드의 불일치를 발견하면 조용히 넘어가지 말고 사용자에게 보고한다.

---

## 3. 확정된 기술 결정 — 변경 금지

아래는 검토를 거쳐 확정된 사항입니다. **바꾸고 싶으면 반드시 먼저 사용자 승인을 받으십시오.**

**서비스 도착점: 소규모 실사용 (지인·베타 수십 명).** 이 전제가 아래 결정 다수의 근거입니다. 배치 사전수집·유료 API·사용자별 rate limit은 **의도적으로 채택하지 않았습니다.** 규모가 커지면 §16.4의 재검토 신호를 따르십시오.

### 3.1 아키텍처

| 항목          | 결정                                                                |
| ------------- | ------------------------------------------------------------------- |
| 프레임워크    | **Next.js (App Router) + TypeScript** 풀스택                        |
| 플랫폼        | **모바일 웹** (반응형 SPA). 네이티브 앱·PWA 아님                    |
| 경로 API      | **카카오모빌리티 길찾기** (무료 일 10,000건)                        |
| 장소 검색     | **카카오 로컬 API** 키워드 검색                                     |
| 지도 렌더링   | **카카오맵 JS SDK** — 주유소 상세 화면에서만                        |
| 주유소·가격   | **오피넷 무료 API** (반경 내 주유소 / 상세정보 / 시군구별 평균가격) |
| 데이터 전략   | **하이브리드** — 마스터는 DB 사전 구축, 후보·가격은 실시간 반경검색 |
| 검색 API 형태 | **SSE 스트리밍** (`POST /api/search`) + **JSON 폴백** (§3.3)        |
| 인증          | **없음.** 로그인 기능을 추가하지 않는다                             |
| AI/LLM        | **사용하지 않는다** (§4)                                            |

### 3.2 스택

| 영역            | 결정                                                 | 비고                                                                            |
| --------------- | ---------------------------------------------------- | ------------------------------------------------------------------------------- |
| 패키지 매니저   | **pnpm**                                             | `packageManager` 필드로 버전 고정                                               |
| DB 접근         | **Drizzle ORM** + `drizzle-kit`                      | 마이그레이션 SQL 파일은 **커밋 대상**                                           |
| DB              | **Neon (관리형 PostgreSQL)**                         | 런타임은 **HTTP 드라이버** `@neondatabase/serverless`. 배치 스크립트는 TCP 가능 |
| 캐시            | **Upstash Redis (REST)**                             | 서버리스에서 커넥션 풀 불필요. 예산 카운터는 `INCRBY`                           |
| 프로비저닝      | **Vercel 마켓플레이스** 경유                         | 접속 정보가 환경변수로 자동 주입                                                |
| 클라이언트 상태 | **Zustand** (+ `persist`)                            | §7.5                                                                            |
| 서버 상태       | **라이브러리 없음.** TanStack Query 보류             | 도입 신호는 §16.4                                                               |
| 스타일링        | **Tailwind CSS**                                     |                                                                                 |
| UI 컴포넌트     | **shadcn/ui — Drawer · Tabs · Switch · Dialog 넷만** | §7.5                                                                            |
| 스키마 검증     | **zod** — `infra/`·`app/api/` 경계 전용              | 도메인·컴포넌트에 들어가지 않음                                                 |
| 테스트          | **Vitest + MSW + Playwright**                        | §10                                                                             |
| 린트·포맷       | **ESLint + Prettier**                                | 레이어 경계 규칙 집행 (§17)                                                     |
| 배포            | **Vercel**                                           |                                                                                 |

### 3.3 SSE와 폴백 — 한 서비스, 두 출구 ★

`recommendation-service`는 **진행 콜백을 옵셔널로 받습니다.** 이 하나의 시그니처가 두 가지를 동시에 해결합니다.

```ts
search(input, onProgress?): Promise<SearchResult>

// 콜백을 넘기면    → SSE 스트리밍 (기본 경로)
// 콜백을 안 넘기면 → 완성된 JSON 하나로 응답 (인앱 브라우저 폴백)
```

**이 인터페이스를 제거하지 마십시오.** 카카오톡 등 인앱 브라우저에서 스트림이 버퍼링되면 폴백이 유일한 경로가 됩니다. 동시에 이 구조가 향후 REST 전환·SSE 재전환의 비용을 반나절 수준으로 유지합니다.

---

## 4. AI/LLM 사용 금지 규칙 ★

**이 서비스의 어떤 계산에도 LLM을 쓰지 않습니다.**

금액·거리·시간은 전부 결정론적으로 계산합니다. 사용자가 "3,252원 이득"을 믿고 18분을 돌아갔는데 그 숫자가 틀리면 그 사용자는 돌아오지 않습니다.

| 대상                                       | 규칙                                                                       |
| ------------------------------------------ | -------------------------------------------------------------------------- |
| `NetSaving`, `TotalCost`, `ΔD`, `ΔT`, 점수 | 순수 함수로만 계산. LLM 호출 금지                                          |
| 추천 이유 문구                             | **템플릿 함수**(`src/domain/reason.ts`)로 생성. 자연어 생성 모델 사용 금지 |
| 후보 정렬·필터                             | 정의된 목적함수로만. 휴리스틱 추가 금지                                    |
| 이상치 판정                                | 통계적 규칙(`OUTLIER_SIGMA`)으로만                                         |

LLM API 클라이언트나 SDK를 이 저장소의 런타임 의존성에 추가하지 마십시오. 필요하다고 판단되면 코드를 쓰기 전에 사용자에게 이유를 설명하고 승인을 받으십시오.

---

## 5. 도메인 계산 불변식 ★

아래는 **깨지면 제품이 거짓말을 하게 되는** 규칙입니다. 리팩터링 중에도 유지하십시오.

1. **`NetSaving`에 시간 비용을 포함하지 않는다.** 시간은 `ΔT`로 화면에 별도 표시하고, 돈으로 환산할지는 사용자가 판단합니다. 시간 환산은 `balanced` 점수 계산 내부에서만 일어납니다.

2. **`d_perp`는 점과 폴리라인 각 _선분_ 까지의 최단거리의 최솟값이다.** 폴리라인 꼭짓점까지의 거리가 아닙니다.

3. **모든 거리 계산은 미터 단위 투영좌표(EPSG:5179)에서 한다.** 위경도(도 단위)로 거리·법선 벡터를 계산하지 마십시오. 위도에 따라 왜곡됩니다.

4. **추정치와 실측치를 절대 섞지 않는다.** `detour.precise`가 `false`인 값을 확정 수치처럼 표시하면 안 됩니다. UI는 반드시 `약 N km ▸` 형태로 구분합니다.

5. **`ΔD`, `ΔT`는 0으로 클램프한다.** 경유지를 넣으면 경로가 재탐색되어 기본 경로보다 짧아지는 경우가 실제로 있습니다. 음수를 그대로 두면 "-3분 우회"가 표시됩니다.

6. **T3는 거리와 이득 두 조건을 모두 만족해야 한다.** `T2_MAX < d_perp ≤ T3_MAX` **그리고** `NetSaving > 0`. 거리 조건만으로 T3에 넣지 마십시오.
   - 단 이 게이트는 **추정치(`ΔD̂ = DETOUR_ESTIMATE_FACTOR × d_perp`) 기준으로 목록 진입 시점에 한 번만** 적용합니다. 정밀 계산 후 `NetSaving`이 음수로 뒤집힌 후보는 **제거하지 않고 회색 처리**합니다 (`PRODUCT.md` §10.1 A7). 이미 보여준 항목이 갑자기 사라지면 사용자가 혼란스러워합니다.

7. **T1·T2에는 `NetSaving` 게이트를 적용하지 않는다.** 손해인 후보도 회색으로 표시합니다. "여기 들르면 손해"를 아는 것도 정보입니다.

8. **`P_ref`의 출처를 결과에 항상 담는다.** `MEDIAN_T1T2`인지 `SIGUNGU_AVG`인지 응답에 포함하고 화면에 기준을 밝힙니다. 기준을 숨긴 절감액은 과장 광고입니다.

---

## 6. UI 불변식 — 제거 금지

아래 요소는 이 제품의 정체성이거나 법적·신뢰 리스크 방어입니다. "간결하게 만들자"는 이유로 지우지 마십시오.

**문구 원문은 [`docs/PRODUCT.md`](docs/PRODUCT.md) §5가 소유합니다.** 여기서는 "무엇을 지우면 안 되는가"만 규정합니다.

| 요소                             | 위치                                 | 이유                                                         |
| -------------------------------- | ------------------------------------ | ------------------------------------------------------------ |
| **확장 고지 배너**               | 결과 목록 상단                       | 이 문장이 뜨는 순간이 곧 제품의 존재 이유 (§5.3 ②)           |
| **가격 기준시각**                | 모든 가격 표시 옆                    | 오피넷은 하루 6회 갱신. 실시간이 아님                        |
| **`오래된 정보` 배지**           | 기준시각 `PRICE_STALE_HOURS` 초과 시 |                                                              |
| **계산 전제 표시**               | 결과 목록 하단 고정                  | 기준가·연비·주유량을 밝히지 않은 절감액은 과장 광고 (§5.3 ⑤) |
| **면책 문구**                    | 상세 화면                            | 가격이 실제와 다를 수 있음 (§5.4)                            |
| **T3 전화 확인 권고 + 전화번호** | 상세 화면 (T3만)                     | 먼 거리를 우회한 뒤 폐업이면 신뢰가 끝남                     |
| **티맵 버튼 안내**               | 딥링크 영역                          | 티맵은 경유지를 전달할 수 없음 (§5.5)                        |
| **후보 0건 시 대안 제시**        | 실패 화면                            | 빈 화면으로 끝내면 우리가 비판한 기존 서비스와 같아짐        |

---

## 7. 코드 작성 원칙

### 7.1 레이어 규칙

```
app/          화면·라우트          → services, components 사용 가능
app/api/      HTTP 경계            → services 사용 가능. infra 직접 호출 금지
services/     오케스트레이션        → domain, infra 사용 가능
domain/       순수 계산            → 외부 의존 0. import 금지 대상: fetch, db, redis, next/*
infra/        외부 시스템 어댑터    → domain 타입만 참조
```

- **`src/domain/`은 순수 함수만 둡니다.** 네트워크·DB·환경변수·`Date.now()` 직접 호출 금지. 시각이 필요하면 인자로 받으십시오. 테스트 가능성이 이 제품 신뢰도의 근간입니다.
- `app/api/`의 route handler는 **파싱 → 서비스 호출 → 직렬화**만 합니다. 비즈니스 로직을 넣지 마십시오.

### 7.2 파라미터

**튜닝 파라미터는 `src/domain/params.ts` 한 곳에만 존재합니다.** 값의 목록과 기본값은 [`docs/PRODUCT.md`](docs/PRODUCT.md) §9가 소유합니다.

거리 임계값·연비·주유량·시간가치·개수 상한 같은 숫자를 `params.ts` 밖에 직접 쓰지 마십시오. 매직 넘버를 발견하면 `params.ts`로 옮기십시오.

### 7.3 타입

- `any` 금지. 외부 API 응답은 경계에서 스키마 검증(zod 등) 후 도메인 타입으로 변환합니다.
- 오피넷 응답 필드명(`UNI_ID`, `OS_NM`, `GIS_X_COOR` …)은 **`infra/opinet/` 밖으로 새어나가면 안 됩니다.** 도메인은 `RefuelPoint` 타입만 압니다.
- 좌표 타입을 구분하십시오: `WGS84Point`, `KatecPoint`, `ProjectedPoint`. 서로 대입되지 않게 하십시오. 좌표계 혼동이 이 프로젝트의 1순위 버그 원천입니다.

### 7.4 단위 계약 ★

**저장·전달·함수 인자는 전부 미터(m) · 초(s) · 원(₩ 정수)입니다.**

| 대상                                           | 단위                                 |
| ---------------------------------------------- | ------------------------------------ |
| 거리 (`D_base`, `ΔD`, `d_perp`)                | **미터 (m), 정수**                   |
| 시간 (`T_base`, `ΔT`)                          | **초 (s), 정수**                     |
| 금액 (`price`, `NetSaving`, `TotalCost`, 점수) | **원 (₩), 정수**                     |
| 연비                                           | km/L (사용자가 인지하는 단위 그대로) |
| 시간가치 `V_TIME`                              | 원/분                                |
| 평균 속도 `AVG_SPEED`                          | km/h                                 |

**⚠️ 아래 세 파라미터만 m·s가 아닙니다.** 이들이 들어가는 식에는 반드시 변환항이 붙습니다.

| 파라미터    | 단위  | 붙어야 할 변환                 |
| ----------- | ----- | ------------------------------ |
| 연비 `E`    | km/L  | 거리에 `/1000`                 |
| `V_TIME`    | 원/분 | 시간에 `/60`                   |
| `AVG_SPEED` | km/h  | 거리에 `/1000`, 결과에 `×3600` |

**km·분 변환은 두 곳에서만 일어납니다.**

1. **표시 직전** (`components/`)
2. **`domain/pricing.ts` 내부** — 연비가 km/L, `V_TIME`이 원/분이라 계산 중간에 변환이 필요합니다. **함수 시그니처는 m·s를 받고, 변환은 함수 몸통 안에서 합니다.**

```ts
// ✅ 올바른 시그니처 — 호출자는 항상 m·s를 넘긴다
netSaving(args: { priceRef: number; priceStation: number;
                  refuelAmountL: number; detourDistanceM: number;
                  efficiencyKmPerL: number }): number
// 내부: detourFuelCost = (detourDistanceM / 1000 / efficiencyKmPerL) * priceStation

computeScores(args: { ...; detourDistanceM: number; detourDurationS: number;
                      timeValuePerMin: number }): Scores
// 내부: balanced = totalCost + (detourDurationS / 60) * timeValuePerMin

// ❌ 금지 — 호출자마다 단위를 다르게 넘기게 되고 1000배·60배 오차가 금액에 실린다
netSaving(detourKm: number, ...)
```

- 변수명에 단위를 붙이십시오: `distanceM`, `durationS`, `detourKm`(표시용), `efficiencyKmPerL`.
- **단위가 붙지 않은 숫자 인자를 도메인 함수에 만들지 마십시오.**

### 7.5 프론트엔드 계층 규칙

**상태는 Zustand 스토어 하나에 둡니다.**

| 대상                                                             | 위치   | `persist`         |
| ---------------------------------------------------------------- | ------ | ----------------- |
| 검색 결과 (`baseRoute`·`candidates`·`expansion`·`P_ref`·`phase`) | 스토어 | ❌ **저장 안 함** |
| `vehicle` (연비·주유량·시간가치)                                 | 스토어 | ✅                |
| 필터 · 마지막 선택 연료                                          | 스토어 | ✅                |
| 최근 검색 3건                                                    | 스토어 | ✅                |

- `persist`는 **`partialize`로 위 넷만** 저장합니다. 검색 결과를 저장하지 마십시오 — 오래된 가격이 되살아납니다.
- `localStorage`를 쓸 수 없으면(프라이빗 모드) **메모리 폴백**합니다. 검색은 정상 동작해야 합니다.
- 스토어가 비어 있는 상태로 `/station/:id`에 직접 진입할 수 있습니다. 이 경우 **"검색 컨텍스트 없음"** 으로 처리해 추천 이유·우회 정보·순위를 감추고 가격·시설·주소만 표시합니다. F10에서 들어온 경우와 같은 화면입니다.

**서버 호출은 컴포넌트에서 직접 `fetch` 하지 않습니다.**

```
src/lib/api/
  useSearchStream.ts     ← SSE 소비 + 3초 내 첫 이벤트 없으면 JSON 폴백
  usePlacesSearch.ts     ← 디바운스 + AbortController
  useNearbyStations.ts
  useStationDetail.ts
  useDetour.ts           ← 결과를 스토어의 candidates에 병합
```

컴포넌트는 훅만 압니다. 이 경계가 있어야 나중에 TanStack Query를 도입할 때 화면을 건드리지 않습니다.

**UI 컴포넌트 라이브러리는 넷으로 제한합니다.**

```
shadcn/ui에서 가져오는 것 (이 넷만): Drawer · Tabs · Switch · Dialog
직접 작성: 결과 카드 · 티어 배지 · 확장 고지 배너 · 로딩 단계 표시 · 가격/기준시각 표시
```

다섯 번째 컴포넌트를 추가하려면 **사용자 승인**을 받으십시오. 결과 카드는 정보가 8~10줄 들어가는 커스텀 UI라 기성 컴포넌트로 만들면 [`docs/PRODUCT.md`](docs/PRODUCT.md) §5.3의 표시 규칙을 담지 못합니다.

---

## 8. 보안·쿼터 규칙 ★

1. **`OPINET_CERT_KEY`와 `KAKAO_REST_API_KEY`는 서버에서만 사용합니다.** `NEXT_PUBLIC_` 접두사를 붙이지 마십시오. 클라이언트가 외부 API를 직접 호출하는 코드를 작성하지 마십시오. 키가 노출되면 일일 한도가 즉시 소진됩니다.

2. **카카오 개발자 앱을 새로 만들지 마십시오.** dev·prod가 단일 앱 키를 공유합니다. 이유는 [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md) §5.2에 있습니다.

3. **오피넷 반경검색의 `radius`는 5000을 넘길 수 없습니다.** API 최댓값입니다. 더 넓게 덮어야 하면 검색 지점을 늘리십시오(법선 오프셋 방식, [`docs/PRODUCT.md`](docs/PRODUCT.md) §7.2 STEP 6).

4. **캐시를 우회하지 마십시오.** 외부 API 호출은 반드시 `infra/*/`의 캐시 래퍼를 통합니다. 디버깅 목적의 캐시 무시는 환경변수로만 켜고, 기본값은 항상 캐시 사용입니다.

5. **개인 식별 가능 정보를 저장하지 마십시오.** 로깅은 익명 이벤트만 남깁니다(`ARCHITECTURE.md` §7.3). IP·User-Agent 원문·정확한 출발지 좌표를 DB에 넣지 마십시오. 좌표는 반드시 격자 스냅 후 저장합니다.

---

## 9. 변경 원칙

### API Contract

- `ARCHITECTURE.md` §6에 정의된 요청·응답 필드를 **삭제하거나 이름을 바꾸지 않습니다.**
- 추가는 가능하되 문서를 같은 커밋에서 갱신합니다.
- SSE 이벤트 타입(`progress`/`base_route`/`partial`/`result`/`warning`/`error`)을 늘리거나 줄이지 마십시오.

### DB Schema

- 마이그레이션 없이 스키마를 바꾸지 않습니다.
- **테이블·컬럼 이름을 `gas_station`/`oil_type`처럼 좁게 만들지 마십시오.** `refuel_point`/`energy_type`을 유지합니다. 향후 전기차 충전소 확장 시 마이그레이션을 피하기 위한 의도적 설계입니다.
- 컬럼을 지우기 전에 사용자에게 확인합니다.

### 외부 API

- 새 외부 API를 추가하기 전에 **무료 쿼터·상업적 이용 조건·좌표계**를 먼저 확인하고 `ARCHITECTURE.md` §5에 기록합니다.
- 오피넷 데이터로 수익을 얻는 기능(광고 등)을 추가하기 전에 사용자에게 알리십시오. 한국석유공사와 **사전 협의가 필요합니다.**

---

## 10. 테스트 원칙

**스택: Vitest (단위·통합) · MSW (외부 HTTP 목킹) · Playwright (E2E).**

| 대상                                                 | 도구                        | 요구 수준                                                                                                        |
| ---------------------------------------------------- | --------------------------- | ---------------------------------------------------------------------------------------------------------------- |
| `src/domain/` 전체                                   | Vitest                      | **단위 테스트 필수.** 순수 함수이므로 예외 없음. 커버리지 90% 이상                                               |
| `geo.ts` (`d_perp`, 샘플링, 법선 오프셋, 좌표 변환)  | Vitest                      | 알려진 좌표로 **수치 검증**. 왕복 변환 오차 50m 이내. `d_perp`가 **선분** 기준임을 확인하는 케이스 포함          |
| `pricing.ts` (`netSaving`, `computeScores`, `P_ref`) | Vitest                      | 경계값 포함 — 음수 `NetSaving`, `P_ref` 폴백 단계 전부, 후보 0·1개                                               |
| `reason.ts`                                          | Vitest                      | 템플릿 6개 분기 전부                                                                                             |
| `infra/` 응답 파싱                                   | Vitest + **실 응답 픽스처** | 픽스처가 zod 스키마를 통과하는지. 픽스처는 Phase 0에서 저장                                                      |
| `opinet/budget`                                      | Vitest                      | **상한에서 정확히 막는가.** 동시 호출 포함                                                                       |
| `recommendation-service`                             | Vitest + MSW                | **4경로** — 확장 발동 / 미발동 / 예산 소진 / 부분 실패                                                           |
| `POST /api/search`                                   | Vitest                      | **이벤트 순서 계약** — `base_route` 최초, `result` 최종, `error` 이후 무발신. **JSON 폴백이 같은 결과를 내는지** |
| 딥링크 생성                                          | Vitest                      | URL 문자열 스냅샷                                                                                                |
| 핵심 사용자 흐름                                     | Playwright                  | **2개만** — ① 검색→결과→상세→딥링크 ② 후보 0건 폴백                                                              |
| 표시 전용 컴포넌트 · 스타일                          | —                           | **작성하지 않음**                                                                                                |

**모든 코드를 테스트하는 것이 목표가 아닙니다.** 금액·거리 계산과 외부 응답 파싱, 그리고 예산 가드가 이 서비스 신뢰도의 전부입니다. 그 밖에 시간을 쓰지 마십시오.

**핵심 계산을 수정하고 테스트를 갱신하지 않은 커밋은 만들지 마십시오.**

---

## 11. 요구사항이 불명확할 때

추측해서 구현하지 마십시오. 아래 순서로 행동합니다.

1. `docs/PRODUCT.md` → `docs/ARCHITECTURE.md` 순으로 찾아봅니다.
2. `ARCHITECTURE.md` §12(미해결 검증 항목)에 있는 사항인지 확인합니다. 있다면 **아직 결정되지 않은 것이 정상**이며, 해당 검증을 먼저 수행하거나 사용자에게 결과를 물어야 합니다.
3. 그래도 없으면 **작업을 멈추고 사용자에게 질문합니다.** 질문에는 선택지와 추천안, 추천 이유를 함께 제시합니다.
4. 임시로 진행해야 한다면 코드에 `// DECISION-NEEDED: <내용>` 주석을 남기고, 응답에서 그 사실을 명시적으로 보고합니다.

> 이 문서들은 별도의 기존 기획문서 4종(`오일로드_01_서비스정의` ~ `04_데이터와API`)을 통합해 만들어졌습니다. **원본은 저장소에 없고 Claude 프로젝트에 보관되어 있습니다.** 원본 근거가 필요하면 사용자에게 요청하십시오. 원본과 이 문서가 다르면 **이 문서가 정본입니다** — 통합 과정에서 발견된 오류가 여기서 수정되었습니다.

**절대 하지 말 것:** 불명확한 값을 그럴듯한 기본값으로 채우고 조용히 넘어가기.

---

## 12. 금지사항

- ❌ 금액·거리·시간 계산에 LLM 사용
- ❌ 추천 이유를 자연어 생성 모델로 작성
- ❌ 클라이언트에서 오피넷·카카오 API 직접 호출
- ❌ `NEXT_PUBLIC_OPINET_*`, `NEXT_PUBLIC_KAKAO_REST_*` 환경변수 생성
- ❌ 카카오 개발자 앱 추가 생성
- ❌ 위경도(도 단위)로 거리·법선 벡터 계산
- ❌ `src/domain/`에서 fetch·DB·Redis·`next/*` import
- ❌ 매직 넘버 하드코딩 (`params.ts` 우회)
- ❌ 확장 고지 배너·가격 기준시각·면책 문구 제거
- ❌ 추정치를 확정 수치처럼 표시
- ❌ `NetSaving`에 시간 비용 합산
- ❌ IP·정확한 좌표 등 개인 식별 가능 정보 DB 저장
- ❌ 로그인·회원가입 기능 추가
- ❌ 24시간 영업·셀프 주유·타이어 공기압 필터 추가 (데이터 없음)
- ❌ 구글지도 딥링크 추가 (한국 자동차 내비 미지원)
- ❌ 전기차 충전소 기능 추가 (별도 제품)
- ❌ `recommendation-service`의 진행 콜백 인터페이스 제거 (§3.3 — 폴백 경로가 사라짐)
- ❌ 컴포넌트에서 직접 `fetch` 호출 (§7.5 — `lib/api/` 훅 경유)
- ❌ Zustand `persist`에 검색 결과 저장 (오래된 가격이 되살아남)
- ❌ shadcn/ui 컴포넌트를 승인 없이 다섯 번째부터 추가
- ❌ zod를 `domain/`이나 컴포넌트에서 사용 (경계 전용)
- ❌ `EventSource` 사용 (자동 재연결이 오피넷 예산을 잠식)
- ❌ ESLint 경계 규칙을 `eslint-disable`로 우회
- ❌ 검증 스크립트를 prod 예산으로 실행

---

## 13. 개발 전 확인사항

작업을 시작하기 전에:

- [ ] `docs/PRODUCT.md`에서 해당 기능의 정의를 읽었는가
- [ ] `docs/ARCHITECTURE.md`에서 해당 모듈의 책임을 확인했는가
- [ ] 건드리는 값이 `params.ts`에 있어야 할 값은 아닌가
- [ ] 외부 API 호출을 늘리는 변경인가? 그렇다면 호출 예산([`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md) §5.3)을 초과하지 않는가
- [ ] 좌표를 다루는가? 어느 좌표계인지 타입으로 명확한가

## 14. 개발 후 확인사항

- [ ] `src/domain/`을 건드렸다면 단위 테스트를 갱신했는가
- [ ] `any`를 쓰지 않았는가
- [ ] 거리는 m, 시간은 s, 금액은 원 정수로 다뤘는가
- [ ] API Contract를 바꿨다면 `ARCHITECTURE.md` §6을 같은 커밋에서 갱신했는가
- [ ] 새 환경변수를 추가했다면 `README.md`와 `.env.example`을 갱신했는가
- [ ] 추정치/실측치 구분(`detour.precise`)이 UI까지 전달되는가
- [ ] 문서와 어긋나는 점을 발견했다면 사용자에게 보고했는가

---

## 15. 개발 착수 전 미완료 검증 항목 ⚠️

**미해결 검증 항목의 정본 목록은 [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md) §12입니다.** 번호(①~⑫)는 그 표를 따릅니다.

그중 **코드를 쓰기 전에 반드시 끝내야 하는 것**은 넷입니다.

| #   | 확인 대상                                                            | 막히는 작업                            |
| --- | -------------------------------------------------------------------- | -------------------------------------- |
| ②   | 전국주유소표준데이터의 좌표·시설 컬럼 **및 오피넷 `UNI_ID` 조인 키** | 마스터 DB 구축 전체                    |
| ③   | KATEC ↔ WGS84 변환 정확도 (오차 50m 이내)                            | 모든 거리 계산                         |
| ⑪   | 반경검색 응답에 **가격 기준시각**이 포함되는가                       | 가격 신선도 표시·`refuel_point` 스키마 |

**① 오피넷 일일 호출 한도는 확인되었습니다: 하루 1,500회.** 예상보다 훨씬 낮은 값이라 알고리즘이 아니라 **서비스 규모 자체**를 제약합니다. 확장 발동 시 검색당 39회를 쓰므로 하루 약 38건이 상한입니다 — 하필 확장이 가장 자주 걸리는 세그먼트가 1순위 타깃(LPG 장거리)입니다.

**대응 방향은 결정되었습니다: MVP는 소규모 베타로 출시합니다.** `OPINET_DAILY_BUDGET=1400`, 예산 소진 시 신규 검색만 안내로 막습니다. 유료 API 전환은 지금 하지 않고 **트래픽이 상한에 근접하면 재검토**하는 항목으로 남겨두었습니다. 상세는 [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md) §5.3.1·§5.3.2. **이 결정을 다시 뒤집거나 유료 API 연동을 추가하려면 사용자에게 먼저 확인하십시오.**

**⑥ Vercel 함수 실행 시간 상한은 해결되었습니다.** Fluid compute 기준 Hobby 플랜의 기본값·최댓값이 300초라 8~12초 파이프라인에 여유가 충분합니다. `maxDuration`은 폭주 방지용 상한으로만 설정합니다(20초 권장). **다만 인앱 브라우저 버퍼링은 별개 문제이며, §3.3의 JSON 폴백이 그 대응입니다.**

**⑬ Upstash 무료 티어 일일 명령 수 한도가 신규 확인 항목으로 추가되었습니다.** 캐시 조회가 검색 1건당 수십 회 발생하므로, 여기서 막히면 오피넷과 별개의 두 번째 병목이 생깁니다.

---

## 16. Git 및 변경 관리

### 16.1 브랜치

혼자 개발하지만 **기능 브랜치 + 셀프 머지 PR**을 씁니다. 리뷰가 목적이 아니라 **CI 게이트와 되돌리기 단위 확보**가 목적입니다.

```
main                  항상 배포 가능. 직접 push 금지
feat/<kebab-case>     새 기능        예: feat/tier-classification
fix/<kebab-case>      버그 수정      예: fix/katec-conversion-offset
chore/<kebab-case>    설정·의존성    예: chore/eslint-boundaries
refactor/<kebab-case> 동작 변경 없음
docs/<kebab-case>     문서만
```

**브랜치 하나 = [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md) §10의 Phase 한 단계** 또는 그 안의 독립적인 작업 하나입니다. 여러 Phase를 한 브랜치에 섞지 마십시오.

### 16.2 커밋

Conventional Commits를 씁니다.

```
feat(domain): d_perp 점-선분 거리 계산 추가
fix(infra): KATEC towgs84 파라미터 보정
test(domain): netSaving 경계값 케이스 추가
docs(architecture): 개발 순서를 Phase 기준으로 갱신
chore(ci): 레이어 경계 린트 규칙 추가
```

스코프는 레이어 이름(`domain`·`services`·`infra`·`app`·`ci`·`scripts`)을 씁니다.

### 16.3 머지

- **squash merge.** PR 하나가 되돌리기 단위입니다
- **머지 조건(CI 통과 필수):** 타입 체크 · ESLint · `domain/` 단위 테스트
- CI가 실패한 채로 머지하지 마십시오. 규칙 위반을 기계가 잡는 유일한 지점입니다

### 16.4 결정 재검토 신호 ★

아래 항목은 **"안 한다"가 아니라 "지금은 안 한다"** 입니다. 신호가 관측되면 사용자에게 안건으로 올리십시오. **임의로 도입하지 마십시오.**

| 보류한 것               | 재검토 신호                                                                                              |
| ----------------------- | -------------------------------------------------------------------------------------------------------- |
| **오피넷 유료 API**     | `search_event` 일별 검색 건수가 하루 상한(약 38~115건)에 근접                                            |
| **배치 사전수집**       | 위와 동일. 또는 인기 경로가 데이터로 드러날 때                                                           |
| **TanStack Query**      | 부차 엔드포인트가 5개 초과 / 동일 데이터 반복 조회 발생 / 재시도·백그라운드 리페치를 직접 구현하게 될 때 |
| **사용자별 rate limit** | 특정 클라이언트가 예산을 편중 소모하는 것이 로그로 확인될 때                                             |
| **네이티브 앱·PWA**     | 딥링크 전환율이 인앱 브라우저 제약 때문에 낮다고 측정될 때                                               |

---

## 17. 도구 체인과 규칙 집행

### 17.1 ESLint가 집행하는 것 ★

**린트는 코드 스타일 통일 도구가 아니라 아키텍처 방어선입니다.** 아래 규칙은 §7.1의 레이어 규칙을 기계가 강제하는 형태입니다.

| 대상 디렉터리       | 금지                                                                                                     |
| ------------------- | -------------------------------------------------------------------------------------------------------- |
| `src/domain/**`     | `fetch` · `@/infra/*` · `@/services/*` · `@/app/*` · `next/*` · `drizzle*` · `@upstash/*` · `zod` import |
| `src/domain/**`     | `Date.now()` · `new Date()` 직접 호출 — **시각은 인자로 받습니다**                                       |
| `src/domain/**`     | 매직 넘버 (`params.ts` 우회)                                                                             |
| `src/app/api/**`    | `@/infra/*` 직접 import — 반드시 `services/` 경유                                                        |
| `src/infra/**`      | `@/services/*` · `@/app/*` import                                                                        |
| `src/components/**` | 직접 `fetch` — `lib/api/` 훅 경유 (§7.5)                                                                 |
| 전역                | `any`                                                                                                    |

**이 규칙들을 `eslint-disable`로 우회하지 마십시오.** 우회가 필요하다고 판단되면 코드를 쓰기 전에 사용자에게 이유를 설명하십시오.

### 17.2 명령

```bash
pnpm dev              # 개발 서버
pnpm test             # Vitest
pnpm test:e2e         # Playwright
pnpm lint             # ESLint
pnpm typecheck        # tsc --noEmit
pnpm db:migrate       # drizzle-kit
```

패키지 추가 시 `pnpm add`를 씁니다. **`package.json`에 없는 패키지를 import하면 pnpm이 즉시 실패시킵니다** — 이걸 우회하려 하지 마십시오.
